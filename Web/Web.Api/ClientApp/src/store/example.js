import { defineStore } from 'pinia'

export const useStore = defineStore('example-store', {
    state: () => {
        return {
            loaded: false
        }
    },

    getters: {
        
    },

    actions: {
        
    }
})